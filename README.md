# LinkedIn Clone

## How to build your own..?

1. Clone this repo
1. Install all the dependencies
    ```bash
    npm i
    ```

    ```bash
    npm run build
    ```

        ```bash
        npm run start
        ```
